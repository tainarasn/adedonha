import { Home } from "./screens/Home"
import { Room } from "./screens/Room"
export const routes = {
    home: { name: "home", component: Home },
    new: {},
    update: {},
    room: { name: "room", component: Room },
}
